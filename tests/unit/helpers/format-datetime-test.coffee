`import { formatDatetime } from '../../../helpers/format-datetime'`
`import { module, test } from 'qunit'`

module 'FormatDatetimeHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = formatDatetime 42
  assert.ok result
